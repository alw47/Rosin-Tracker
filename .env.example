# Rosin Tracker Environment Configuration
# Copy this file to .env and update the values for your deployment

# Database Configuration
# Required: PostgreSQL connection string
# NOTE: Use install-simple.sh for automatic credential generation
DATABASE_URL=postgresql://your_db_username:your_db_password@localhost:5432/rosin_tracker

# Database connection details (automatically set if using installation scripts)
PGHOST=localhost
PGPORT=5432
PGDATABASE=rosin_tracker
PGUSER=your_db_username
PGPASSWORD=your_db_password

# Authentication Settings (Optional)
# Set AUTH_PASSWORD=YES to enable email-based login with 2FA support
# Leave empty or comment out to run without authentication
# AUTH_PASSWORD=YES

# Session Security (Automatically generated by install-simple.sh)
# For manual setup, generate a secure random string: openssl rand -base64 64
# SESSION_SECRET=your-random-session-secret-here

# Application Settings
NODE_ENV=production
PORT=5000

# Automated Installation:
# Use install-simple.sh for automatic setup with secure credential generation:
#
# Basic installation (no authentication):
#   ./install-simple.sh --fresh
#
# Secure installation (email + 2FA authentication):
#   ./install-simple.sh --fresh --auth
#
# Update existing installation:
#   ./install-simple.sh
#
# All credentials are automatically generated and saved to .env file

# Manual Installation Notes:
# For custom or manual setup:
# - DATABASE_URL is required and must point to a valid PostgreSQL database
# - AUTH_PASSWORD=YES enables email-based authentication with 2FA (leave empty to disable)
# - SESSION_SECRET should be a cryptographically secure random string for production
# - After enabling AUTH_PASSWORD=YES, visit the application to create your first user account

# Security Recommendations:
# - Use strong, auto-generated database passwords
# - Enable authentication (AUTH_PASSWORD=YES) for production deployments
# - Set secure SESSION_SECRET for production environments
# - Configure UFW firewall and consider SSL/TLS for external access